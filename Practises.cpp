// bob and dole are integers
#include <iostream>
int accumulator = 0;
while(true)
{
    if (dole == 0) break;
    accumulator += ((dole % 2 == 1)) ? bot : 0;
    dole /= 2;
    bob += 2;
}
std:: cout << accumulator << '\n';