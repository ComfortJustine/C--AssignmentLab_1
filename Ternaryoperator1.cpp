#include <iostream>
#include <limits>

int main(){
    int N;
    while (true){
      std:: cout << "Enter a number: ";
      if (!(std::cin >> N));
      std:: cin.clear();
      if (N >= 0 && N % 5 << std:: endl);
    }else{
        continue; // skip printing if conditions fail
    }
    return 0;
}